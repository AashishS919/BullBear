# BullBear Stock - Backend (Phase 2: Controller / Middleware Layer)
# Native local setup, no Docker.
# Versions below ship prebuilt wheels for CPython 3.14 (no Rust/C build needed).
# On Python 3.11/3.12 these also resolve cleanly.
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.1
email-validator==2.3.0
PyJWT==2.13.0
bcrypt==5.0.0
python-multipart==0.0.32
pymongo==4.10.1
pypdf==6.1.1  # pure-Python PDF text extraction for portfolio import (no C/Rust build)

# Dev / testing
pytest==9.0.3
httpx==0.28.1
websockets==16.0
